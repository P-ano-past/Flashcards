// import { useUser } from "../Context/UserContext";

export default function Landing() {
  return (
    <div>
      <div>
        This is the landing page. Look at the very nice copy. Succumb to the
        marketing!
      </div>
      <div>
        If the user is already authenticated, then ship them on over to the home
        screen so they can do what they need to do.{" "}
      </div>
    </div>
  );
}
